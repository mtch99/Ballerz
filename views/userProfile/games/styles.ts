import {StyleSheet} from "react-native"


const styles = StyleSheet.create({
    container: {
        
    },

    gamesText: {
        fontSize: 16,
        color: "#595085",
        // marginHorizontal: 20
    },

})

export default styles