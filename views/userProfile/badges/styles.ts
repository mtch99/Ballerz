import { StyleSheet } from "react-native"

const styles = StyleSheet.create({
    container: {
        marginHorizontal: 20,
        // marginTop: 40,
    },

    badgeText: {
        fontSize: 16,
        color: "#595085"
    },

    badgeListContainer: {
        flexDirection: 'row',
    },

    badgeItem: {
        fontSize: 26,
        marginRight: 0.5
    }


})

export default styles