import { StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container: {
        marginHorizontal: 20,
        // flexDirection: 'row',
        marginVertical: 15
    },

    mediaText: {
        fontSize: 16,
        color: "#595085"
    },

    mediaListContainer: {
        marginTop: 5
    },

    pictureItem: {
        height: 100,
        width: 100,
        marginLeft: 5
    }
})

export default styles;