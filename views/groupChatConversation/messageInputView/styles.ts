import { StyleSheet} from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex:1,
        marginBottom: 4,
        alignSelf: "center",
    },

    textInputContainer: {
        flexGrow:1,
        height: '60%',
        marginBottom: 4,
        flexDirection: "row",
    },
})

export default styles